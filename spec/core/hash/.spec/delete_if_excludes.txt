Hash#delete_if raises TypeError if called on a frozen instance
Hash#delete_if raise RuntimeError if #rehash is called from block
Hash#delete_if raises if more than 63 new entries are added from block
