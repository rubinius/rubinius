unstable(start_server causes random hangs, especially on amd64):Net::HTTP.start returns a new Net::HTTP object for the passed address and port
unstable(start_server causes random hangs, especially on amd64):Net::HTTP.start opens the tcp connection
unstable(start_server causes random hangs, especially on amd64):Net::HTTP.start when passed a block returns the blocks return value
unstable(start_server causes random hangs, especially on amd64):Net::HTTP.start when passed a block yields the new Net::HTTP object to the block
unstable(start_server causes random hangs, especially on amd64):Net::HTTP.start when passed a block opens the tcp connection before yielding
unstable(start_server causes random hangs, especially on amd64):Net::HTTP.start when passed a block closes the tcp connection after yielding
unstable(start_server causes random hangs, especially on amd64):Net::HTTP#start returns self
unstable(start_server causes random hangs, especially on amd64):Net::HTTP#start opens the tcp connection
unstable(start_server causes random hangs, especially on amd64):Net::HTTP#start when self has already been started raises an IOError
unstable(start_server causes random hangs, especially on amd64):Net::HTTP#start when passed a block returns the blocks return value
unstable(start_server causes random hangs, especially on amd64):Net::HTTP#start when passed a block yields the new Net::HTTP object to the block
unstable(start_server causes random hangs, especially on amd64):Net::HTTP#start when passed a block opens the tcp connection before yielding
unstable(start_server causes random hangs, especially on amd64):Net::HTTP#start when passed a block closes the tcp connection after yielding
