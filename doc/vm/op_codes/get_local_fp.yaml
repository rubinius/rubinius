---
mnemonic: get_local_fp
operation: >-

format: get_local_fp <arg1>
opcode: 100
stack_before:
  - <consumed>
  - ...
stack_after:
  - <produced>
  - ...
description: >-

source: |-
    next_int;
    assert(c->sp_ptr > c->fp_ptr + _int);
    stack_push(*(c->fp_ptr + _int));
