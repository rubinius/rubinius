---
mnemonic: set_const
operation: >-

format: set_const <arg1>
opcode: 25
stack_before:
  - <consumed>
  - ...
stack_after:
  - <produced>
  - ...
description: >-

source: |-
    next_int;
    t1 = tuple_at(state, cpu_current_literals(state, c), _int);
    stack_push(cpu_const_set(state, c, t1, stack_pop(), c->enclosing_class));
