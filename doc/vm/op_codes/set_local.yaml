---
mnemonic: set_local
operation: >-

format: set_local <arg1>
opcode: 17
stack_before:
  - <consumed>
  - ...
stack_after:
  - <produced>
  - ...
description: >-

source: |-
    next_int;
    t1 = stack_pop();
    // printf("Set local %d to %s\n", _int, _inspect(t1));
    tuple_put(state, cpu_current_locals(state, c), _int, t1);
    stack_push(t1);
