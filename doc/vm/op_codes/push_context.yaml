---
mnemonic: push_context
operation: >-
  Pushes the current method context onto the stack
format: push_context
opcode: 10
stack_before:
  - ...
stack_after:
  - methodctxt
  - ...
description: >-
  Creates a reference to the current method execution context, and pushes it
  onto the stack.
source: |-
    methctx_reference(state, c->active_context);
    stack_push(c->active_context);
