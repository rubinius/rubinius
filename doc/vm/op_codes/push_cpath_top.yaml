---
mnemonic: push_cpath_top
operation: >-
  Pushes the top level global object onto the stack
format: push_cpath_top
opcode: 54
stack_before:
  - ...
stack_after:
  - cpathtop
  - ...
description: >-
  Pushes the top-level global object that represents the top-level namespace
  for constants. Used when a global variable is referenced.
example: >-
  puts $:    # Global variables are looked up on the top-level Globals object
source: |-
  stack_push(global->object);
