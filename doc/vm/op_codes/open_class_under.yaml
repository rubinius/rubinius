---
mnemonic: open_class_under
operation: >-

format: open_class_under <arg1>
opcode: 31
stack_before:
  - <consumed>
  - ...
stack_after:
  - <produced>
  - ...
description: >-

source: |-
    int created;
    t1 = stack_pop();
    t2 = stack_pop();
    t3 = cpu_open_class(state, c, t2, t1, &created);
    if(t3 != Qundef) {
      stack_push(t3);
      if(created) cpu_perform_hook(state, c, t3, state->global->sym_opened_class, t1);
    }
