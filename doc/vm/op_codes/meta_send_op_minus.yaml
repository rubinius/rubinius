---
mnemonic: meta_send_op_minus
operation: >-

format: meta_send_op_minus
opcode: 79
stack_before:
  - <consumed>
  - ...
stack_after:
  - <produced>
  - ...
description: >-

source: |-
    t1 = stack_pop();
    t2 = stack_pop();
    if(FIXNUM_P(t1) && FIXNUM_P(t2)) {
      stack_push(fixnum_sub(state, t1, t2));
    } else {
      stack_push(t2);
      _lit = state->global->sym_minus;
      t2 = Qnil;
      j = 1;
      goto perform_send;
      // cpu_send_method(state, c, t1, state->global->sym_minus, 1);
    }
